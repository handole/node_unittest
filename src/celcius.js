// celcius to reamur
export const reamur = (cel) => {
    return (4 / 5) * cel;
};

// celcius ke kelvin
export const kelvin = (cel) => {
    return cel + 273;
}

// celcius ke fahrenheit
export const fahrenheit = (cel) => {
    return (9 / 4) * cel + 32;
}
